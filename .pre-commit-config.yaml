repos:
  - repo: local
    hooks:
      - id: tox
        name: tox
        entry: uv run tox -e lint,type
        language: system
        types: [python]
        pass_filenames: false
        always_run: true
